# Application environment
# Possible values: dev | prod
ENV=dev

# Database connection (SQLAlchemy URL)
#
# Local example:
# postgresql+psycopg://user:password@localhost:5432/dbname
#
# Production example (Neon / Render):
# postgresql+psycopg://user:password@ep-xxxx-pooler.sa-east-1.aws.neon.tech/dbname?sslmode=require
DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/qshift

# CORS configuration
# List of allowed origins (JSON array)
# Dev: localhost
# Prod: frontend domain only
ALLOWED_ORIGINS=["http://localhost:5173"]

# Authentication mode
AUTH_MODE=jwt

# JWT secret key
# Generate with:
# python -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=CHANGE_ME

# JWT expiration time (in minutes)
ACCESS_TOKEN_EXPIRE_MINUTES=100

# JWT signing algorithm
JWT_ALGORITHM=HS256

# Schedule generation integration
# Base URL of the schedule generator API
SCHEDULE_GENERATOR_BASE_URL=http://localhost:8001

# Base URL of the core API used by the generator callback
CORE_API_BASE_URL=http://localhost:8000

# Shared secret used to sign generator callbacks
SCHEDULE_CALLBACK_SECRET=CHANGE_ME
